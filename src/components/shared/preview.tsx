import Image from 'next/image';
import React from 'react'

const Preview = ({ imageUrl, imageHeight, imageWidth }: { imageUrl: string, imageWidth: number, imageHeight: number }) => {
    return (
        <div className="w-full mt-8">
            preview
        </div>
    )
}

export default Preview;